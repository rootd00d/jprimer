---
version: '3.2'
services:
  server:
    build:
      context: ./server
    environment:
      NODE_ENV: development
    ports:
      - "3000:3000"
      - "9229:9229"
    command: ["npm", "run", "debug"]
    volumes:
      - ./server:/usr/src/app
